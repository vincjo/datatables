
# Invalidate

The invalidate method runs your load function and updates table's data.

```ts
table.invalidate()
```

Most of the time, the table handler automatically runs invalidate. Use it with caution.