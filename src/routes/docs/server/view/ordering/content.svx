
# Column ordering

Column re-ordering requires drag-and-drop events that don't need to rely on the `TableHandler` instance.

<a href="/datatables/examples/client/column-ordering" rel="external">Here is an example implementing *svelte-dnd-action*.</a>


<style>
    a {
        color: var(--ternary);
    }
</style>