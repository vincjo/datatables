Return rows regardless of pagination.

This method should not be very useful unless for special calculation requirements. In most cases it'll be easier to use the `createCalculation()` method instead.